# Runtime dependencies for the Matrix Studio add-on.
# Lower bounds are the oldest releases publishing cp313 musllinux_1_2 wheels
# for aarch64/amd64, so the image builds without a compiler (see Dockerfile).
aiohttp>=3.10,<4
numpy>=2.1,<3
Pillow>=11.0,<13
